import { ObjectStyles } from "@tipos/component";

export const FormStyle: ObjectStyles = {
    form: {
        display: "flex",
        flexDirection: "column",
        gap: "1rem",
        width: "300px",
    }
}
